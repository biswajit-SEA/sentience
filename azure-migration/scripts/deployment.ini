# Deployment Configuration

# Python version
python_version = "3.10"

# Deployment command to run during deployment
startup_command = "gunicorn --bind=0.0.0.0:5000 hackathon:app"

# Runtime configuration for Azure App Service
runtime = "python"
version = "3.10"

# Deployment source
repo_url = "https://github.com/yourusername/techm-hackathon.git"
branch = "main"

# Health check path
health_check_path = "/health"
